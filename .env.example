# Optional feedback destination. Email takes priority if both are set.
NEXT_PUBLIC_FEEDBACK_EMAIL=
# Format: owner/repository (repository must accept visitor issues).
NEXT_PUBLIC_FEEDBACK_GITHUB_REPO=
